# ANATOMICA PWA icons

The PNG icons in this directory are generated by `scripts/generate-icons.mjs`.
They are intentionally committed so a production build has install icons even
before the postinstall hook runs. Re-run `npm run pwa:icons` after changing the
icon artwork.
